<% include partials/header %>

<!-- <h1>This is the show page</h1>

<h3><%= blog.title %></h3>
<img src=<%= blog.image %> alt="">
<p><%= blog.content %></p>
<button><a href="/blogs/<%= blog._id %>/edit">EDIT</a></button>
<button><a href="/blogs/<%= blog._id %>/delete">DELETE</a></button> -->

<div class="ui inverted segment" id="show-segment">
    <h2><%= blog.title %></h2>
        <div class="ui inverted divider"></div>
    <div class="ui segment inverted">
    <div class="ui two column very relaxed grid">
        <div class="column">
            <img src=<%= blog.image %>>
        </div>
        <div class="column">
            <p><%- blog.content %></p>
        </div>
    </div>
    <div class="ui vertical divider"></div>
    </div>
        <div class="ui inverted divider"></div>
    <span><%= blog.created.toDateString() %></span>
</div>

<!-- <div class="ui form">
<div id="show-buttons" class="inline fileds">
    <div class="field">
        </div>
    <div class="field">
        </div>
    <div class="field">
        
    </div>
</div>
</div> -->

<div class="ui form" id="show-buttons">
    <div class="inline fields">
        <div class="field">
            <a href="/blogs/<%= blog._id %>/edit"><button class="ui inverted button">Edit Post</button></a>
        </div>
        <div class="field">
            <a href="/blogs"><button class="ui inverted grey button">Back</button></a>
        </div>
        <div class="field">
            <form action="/blogs/<%= blog._id %>?_method=DELETE" method="post"><a href="/blogs/<%= blog._id %>">
                <button class="ui inverted black button"> Delete Post</button></a>
            </form>
        </div>
    </div>
</div>
      
<% include partials/footer %>

